apiVersion: v1
kind: Service
metadata:
  name: {{ include "config-manager.fullname" . }}-api
  labels:
    {{- include "config-manager.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "config-manager.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http

