PORT=8080
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/config_manager?sslmode=disable

# Alternatively (password-only secret friendly):
DB_HOST=localhost
DB_PORT=5432
DB_NAME=config_manager
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSLMODE=disable
